# 后端部署流程

适用范围：`/Users/lee/Documents/code/dati/backend`

## 1. 依赖前提

- 本地已安装 `mvn`
- 本地已安装 `python3`
- 本地已安装 `paramiko`，或者已安装 `sshpass`
- 服务器可通过账号密码登录
- 线上服务已预先完成：
  - Java 17
  - `dati-backend.service`
  - Nginx 反代
  - MySQL 与 Flyway 建表

## 2. 日常发布

发布后端只需要执行：

```bash
cd /Users/lee/Documents/code/dati/backend
bash scripts/deploy.sh
```

等价写法：

```bash
bash scripts/deploy.sh deploy
```

这个流程会做：

1. 本地执行 `mvn -DskipTests package`
2. 上传新的 `target/dati-backend-0.0.1-SNAPSHOT.jar`
3. 重启服务器上的 `dati-backend.service`
4. 校验内网健康与公网 HTTPS 接口

## 3. 只改配置

如果只修改了数据库连接、端口、JWT、systemd 配置，执行：

```bash
cd /Users/lee/Documents/code/dati/backend
bash scripts/deploy.sh config
```

这个流程会：

1. 重新生成 `application.yml`
2. 重新生成 `dati-backend.service`
3. 上传到服务器
4. 重启服务

## 4. 公网地址

当前后端公网入口：

```text
https://webfeng.org/dati-api
```

常用接口示例：

```text
https://webfeng.org/dati-api/api/auth/wechat-login
https://webfeng.org/dati-api/api/home
https://webfeng.org/dati-api/api/questions/sequence
```

## 5. 服务器文件位置

- 应用 JAR：`/opt/dati-backend/dati-backend.jar`
- 配置文件：`/etc/dati-backend/application.yml`
- systemd：`/etc/systemd/system/dati-backend.service`
- Nginx 站点：`/etc/nginx/sites-enabled/webfeng`

## 6. 验证方式

发布后可手工检查：

```bash
systemctl status dati-backend.service
curl -k https://webfeng.org/dati-api/api/auth/wechat-login
```

如果 `deploy` 成功，脚本最后会输出 `done`。

## 7. 说明

- 当前脚本默认使用本地 `PASS` 变量连接服务器，必要时可在命令前覆盖环境变量
- 脚本不会重复安装 Java，也不会重复修改 Nginx
- 线上数据库的等级已经合并为 `初级 / 中级 / 高级`
