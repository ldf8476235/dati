<view class="app-nav" style="{{navStyle}}">
  <view wx:if="{{backHome}}" class="app-nav-left" style="{{leftStyle}}" bindtap="goHome">
    <text class="app-nav-back">‹</text>
  </view>
  <view class="app-nav-title" style="{{titleStyle}}">{{title}}</view>
</view>
